AZURE_AI_PROJECT_ENDPOINT=
AZURE_AI_MODEL_DEPLOYMENT_NAME=gpt-4.1

# Local model backend: "mlx" (default, Apple Silicon) or "transformers" (cross-platform)
LOCAL_BACKEND=mlx
# Model path override (HuggingFace model ID or local path)
# LOCAL_MODEL_PATH=Phi-4-mini-instruct-4bit