python-dotenv
agent-framework==1.12.1
# agent-framework-mlx pins agent-framework-core==1.3.0 exactly in its own metadata,
# which conflicts with the agent-framework version above. It must be installed with
# `pip install --no-deps` (see build.sh / .github/workflows/build.yml). Its real
# runtime dependencies (mlx, mlx-lm) are listed below so they still resolve normally.
agent-framework-mlx==0.6.0
mlx==0.31.1
mlx-lm==0.31.1
agent-framework-foundry-local
azure-identity
azure-ai-projects==2.1.0
azure-ai-agents==1.2.0b5
opentelemetry-semantic-conventions-ai==0.4.13
pydantic>=2.0.0
